let Nome = prompt ("Seu primeiro nome por gentileza: ");

let Sobrenome = prompt ("Agora seu sobrenome: ");

let NomeCompleto = `Seu Nome é ? : ${Nome} ${Sobrenome}.`

alert (NomeCompleto);

alert("Olá Bem Vindo ao GitHub")

alert("Agora uma Frase para refletir!");
alert("“Por maior que seja, não há obstáculo que não possa ser superado”!");